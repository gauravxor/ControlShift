spring:
  application.name: player-service
  datasource:
    url: jdbc:postgresql://localhost:5434/playerdb
    username: playeruser
    password: playerpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8000